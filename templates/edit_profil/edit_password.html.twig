{% extends 'base.html.twig' %}

{% block title %}Modification Du Mot de Passe{% endblock %}

{% block body %}

    <div class="container">
    {% for message in app.flashes('warning') %}
      <div class="alert alert-warning">
        {{ message }}
      </div>
    {% endfor %}
    </div>

    <h1 class="text-center">Formulaire de modification du mot de passe</h1>

    <div class="container">
        {{ form(form) }}
    </div>

{% endblock %}
