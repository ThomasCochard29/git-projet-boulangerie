{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

  {#! Macarons #}
  <p class="text-center h1">{{controller_macaron}}</p>

  <div class="container mx-auto mt-3">
    <div class="row">
    {% for produits in produits %}
      <div class="col-md-3">
        <div class="card" style="width: 18rem;">
          <img src="uploads/{{produits.Image}}" class="card-img-top" alt="...">
          
          <div class="card-body">
            <h5 class="card-title">{{produits.nom}}</h5>
            <p class="card-text">{{produits.prix}}</p>
          </div>
        </div>
      </div> 
    {% endfor %}
    </div> 
  </div> 

  {#! Chocolats #}
  <p class="text-center h1">{{controller_chocolat}}</p>

  <div class="container mx-auto mt-3">
    <div class="row">
    {% for produits in produits %}
      <div class="col-md-3">
        <div class="card" style="width: 18rem;">
          <img src="uploads/{{produits.Image}}" class="card-img-top" alt="...">
          
          <div class="card-body">
            <h5 class="card-title">{{produits.nom}}</h5>
            <p class="card-text">{{produits.prix}}</p>
          </div>
        </div>
      </div> 
    {% endfor %}
    </div> 
  </div> 

  {#! Confiseries #}
  <p class="text-center h1">{{controller_confiserie}}</p>

  <div class="container mx-auto mt-3">
    <div class="row">
    {% for produits in produits %}
      <div class="col-md-3">
        <div class="card" style="width: 18rem;">
          <img src="uploads/{{produits.Image}}" class="card-img-top" alt="...">
          
          <div class="card-body">
            <h5 class="card-title">{{produits.nom}}</h5>
            <p class="card-text">{{produits.prix}}</p>
          </div>
        </div>
      </div> 
    {% endfor %}
    </div> 
  </div> 


{% endblock %}
